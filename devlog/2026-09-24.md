# 学習ログ｜2026-09-24

## 今日やったこと

`AddPatientForm` のテストを完成させ、次の4件がすべて通過した。

- `showAddForm` が `false` のとき、フォームを表示しない
- `showAddForm` が `true` のとき、フォームを表示する
- 「キャンセル」を押すと、フォームを閉じる処理を呼ぶ
- 「保存」を押すと、入力値を `onSubmit` に渡す

実行結果：**1ファイル、4テスト通過**

## 今日のポイント

`vi.fn()` で作った `onSubmit` をフォームに渡すと、保存時に呼ばれた引数を検証できる。

```tsx
const onSubmit = vi.fn();

// フォームに onSubmit={onSubmit} を渡す

expect(onSubmit).toHaveBeenCalledWith({
  name: "testName",
  room: 999,
});